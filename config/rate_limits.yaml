# Rate Limiter Configuration for CyberScout AI

default:
  delay_seconds: 1.0
  max_requests_per_minute: 60

sources:
  github_search:
    delay_seconds: 2.0
    max_requests_per_minute: 30
  ctftime:
    delay_seconds: 1.0
    max_requests_per_minute: 60
  thehackernews:
    delay_seconds: 0.5
    max_requests_per_minute: 120
  bleepingcomputer:
    delay_seconds: 0.5
    max_requests_per_minute: 120
  tryhackme:
    delay_seconds: 3.0
    max_requests_per_minute: 10
  portswigger_academy:
    delay_seconds: 3.0
    max_requests_per_minute: 10
